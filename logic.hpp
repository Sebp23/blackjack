#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <chrono>

enum GameOutcome {LOSE, WIN};

class Card;

struct Deck;

struct Dealer;

struct Player;

struct PlayGame;